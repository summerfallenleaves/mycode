# mycode 手动功能测试计划

> 版本: v1.0  
> 日期: 2026-06-09  
> 前置条件: `mycode` 已全局安装，配置文件 `.mycode/mycode.jsonc` 中至少有一个可用的 LLM Provider

---

## 测试环境准备

```bash
# 1. 确保在项目根目录
cd /Users/summer_tears/AI/git_repo/mycode

# 2. 构建最新版本
pnpm build

# 3. 安装全局命令
bash scripts/install-cli.sh

# 4. 启动 mycode
mycode
```

---

## 一、基础对话功能

### TEST-1.1: 简单问答

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1.1.1 | 输入 `你好，请介绍一下你自己` 回车 | 状态栏显示 `Running...`，流式输出 Agent 自我介绍文本 |
| 1.1.2 | 等待响应完成 | 状态栏变为 `Ready` |
| 1.1.3 | 输入 `1+1等于几？` 回车 | 正确回答 `2` |

### TEST-1.2: 多轮对话

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1.2.1 | 输入 `我的名字叫小明` 回车 | Agent 确认或回应 |
| 1.2.2 | 输入 `我叫什么名字？` 回车 | Agent 回答 `小明` |

### TEST-1.3: 流式输出

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 1.3.1 | 输入 `写一首关于春天的五言绝句` 回车 | 文本逐字/逐 token 流式出现，无明显闪烁 |

---

## 二、内置工具测试

### TEST-2.1: read_file

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 2.1.1 | 输入 `读取 README.md 文件内容` 回车 | ① 状态栏显示 `Running...` ② 看到 `✓ read_file README.md` 工具调用 ③ 工具结果显示文件路径和行数 ④ 最终回答包含文件内容摘要 |

### TEST-2.2: write + read_file

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 2.2.1 | 输入 `在 /tmp/mycode-test.txt 文件中写入 "Hello from mycode test"` 回车 | 工具执行成功 |
| 2.2.2 | 输入 `读取 /tmp/mycode-test.txt` 回车 | 内容为 `Hello from mycode test` |
| 2.2.3 | 清理: 输入 `删除 /tmp/mycode-test.txt` 回车 | 文件删除成功 |

### TEST-2.3: edit

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 2.3.1 | 输入 `把 README.md 中的 "从零开始构建" 替换为 "从零构建"` 回车 | 工具显示替换成功，显示原字符串 → 新字符串 |

### TEST-2.4: bash

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 2.4.1 | 输入 `执行 ls -la 命令查看当前目录` 回车 | ① 看到 `✓ bash` 工具调用 ② 显示命令和输出 ③ 回答包含目录列表 |
| 2.4.2 | 输入 `查看今天的日期` 回车 | Agent 调用 `date` 命令返回日期 |

### TEST-2.5: grep

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 2.5.1 | 输入 `搜索项目中所有包含 "Agent" 的 TypeScript 文件` 回车 | 工具显示匹配数量和文件列表 |

### TEST-2.6: glob

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 2.6.1 | 输入 `列出 packages 目录下所有 package.json 文件` 回车 | 工具显示匹配文件列表 |

### TEST-2.7: question（用户交互）

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 2.7.1 | 输入 `问我一个问题，让我选择喜欢的编程语言` 回车 | ① 弹出选项面板，列出编程语言选项 ② 输入选项编号（如 `1`）回车 ③ Agent 根据选择回应 |

### TEST-2.8: todowrite

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 2.8.1 | 输入 `帮我创建一个待办列表：1. 重构代码 2. 写测试 3. 更新文档` 回车 | ① 工具执行成功 ② 右侧 Todo 侧边栏显示 3 个待办项 |

### TEST-2.9: memory

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 2.9.1 | 输入 `记住：我喜欢用 TypeScript 开发` 回车 | 工具执行成功 |
| 2.9.2 | 输入 `搜索 memory 中关于 TypeScript 的内容` 回车 | 返回之前存储的记忆 |

---

## 三、CLI 命令测试

### TEST-3.1: /models — 切换模型

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 3.1.1 | 输入 `/models` 回车 | 弹出模型选择面板，显示所有已配置 Provider |
| 3.1.2 | 按 `↓` 选择另一个 Provider | 高亮移动到下一个 |
| 3.1.3 | 按 `Enter` 确认 | 左上角 Provider 名称更新 |
| 3.1.4 | 按 `Esc` 重新打开面板 | 可取消选择 |

### TEST-3.2: /skills — 技能列表

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 3.2.1 | 输入 `/skills` 回车 | 弹出技能列表面板，显示名称、描述、路径 |
| 3.2.2 | 按 `Esc` 关闭 | 面板消失 |

### TEST-3.3: /mcps — MCP 服务器状态

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 3.3.1 | 输入 `/mcps` 回车 | 显示已配置 MCP 服务器列表及连接状态 |
| 3.3.2 | 按 `Esc` 关闭 | 面板消失 |

### TEST-3.4: /connect — 添加 Provider

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 3.4.1 | 输入 `/connect` 回车 | 进入连接向导，提示选择格式 |
| 3.4.2 | 选择 `openai` | 进入 Base URL 输入 |
| 3.4.3 | 输入 `https://api.example.com` 回车 | 进入模型名输入 |
| 3.4.4 | 输入 `gpt-test` 回车 | 进入 API Key 输入 |
| 3.4.5 | 输入 `sk-test` 回车 | 进入名称输入 |
| 3.4.6 | 输入 `test-provider` 回车 | 提示成功，Provider 列表中可见 |
| 3.4.7 | 按 `Esc` 任意步骤 | 取消向导 |

### TEST-3.5: /new — 新对话

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 3.5.1 | 先输入一些对话后，输入 `/new` 回车 | 清空所有历史消息，状态栏事件计数归零 |

### TEST-3.6: /compact — 手动压缩上下文

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 3.6.1 | 进行多轮对话（5+ 轮）后，输入 `/compact` 回车 | 状态栏显示上下文使用率下降 |
| 3.6.2 | 继续对话 | 之前对话内容以 `[此前对话已压缩]` 摘要形式存在 |

### TEST-3.7: /remember + /memory + /forget

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 3.7.1 | 输入 `/remember 项目使用 Biome 做格式化` 回车 | 状态栏提示记忆已保存 |
| 3.7.2 | 输入 `/memory` 回车 | 显示已存储的记忆列表（包含 ID 和类型） |
| 3.7.3 | 输入 `/forget` 回车 | 列出可删除的记忆条目 |
| 3.7.4 | 输入 `/forget <id前缀>` 回车 | 提示删除成功 |
| 3.7.5 | 再次输入 `/memory` 回车 | 确认该条记忆已被移除 |

### TEST-3.8: /rule — 项目规则

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 3.8.1 | 输入 `/rule 所有 API 请求都需要添加 Authorization 头` 回车 | 规则追加成功 |
| 3.8.2 | 输入 `/new` 然后 `项目中 API 请求的约定是什么？` | Agent 回答包含规则内容 |

### TEST-3.9: /resume — 恢复会话

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 3.9.1 | 进行对话后，输入 `/exit` 记下 sessionId | 终端显示 `恢复会话：mycode -c <sessionId>` |
| 3.9.2 | 重新启动 `mycode` | 新会话 |
| 3.9.3 | 输入 `/resume` 回车 | 显示历史会话列表（ID 前 8 位 + 消息数） |
| 3.9.4 | 选择之前的会话，按 `Enter` | 恢复历史消息 |
| 3.9.5 | 输入 `继续我们之前的话题` | Agent 能基于历史上下文回应 |

### TEST-3.10: /resume 命令行方式

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 3.10.1 | 记下 sessionId，退出 | — |
| 3.10.2 | `mycode -c <sessionId>` | 直接恢复会话 |
| 3.10.3 | 验证历史消息显示 | 之前的对话可见 |

### TEST-3.11: /exit — 退出

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 3.11.1 | 有活跃会话时，输入 `/exit` 回车 | 终端显示 `恢复会话：mycode -c <sessionId>`，程序退出 |
| 3.11.2 | 无活跃会话时（`/new` 后未发言），输入 `/exit` | 显示 `没有活跃会话`，程序退出 |

---

## 四、Shell 命令和错误处理

### TEST-4.1: ! 前缀执行 Shell

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 4.1.1 | 输入 `!echo hello` 回车 | 弹出 shell 输出面板，显示 stdout |
| 4.1.2 | 按 `Esc` 关闭 | 面板消失 |

### TEST-4.2: 未知命令

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 4.2.1 | 输入 `/nonexistent` 回车 | 显示红色未知命令提示 |
| 4.2.2 | 按任意键关闭 | 面板消失 |

### TEST-4.3: 空输入

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 4.3.1 | 直接按回车（空输入） | 无反应，输入框清空 |

### TEST-4.4: 自动补全

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 4.4.1 | 输入 `/mod` 按 `Tab` | 自动补全为 `/models` |
| 4.4.2 | 按 `Enter` | 执行命令 |

---

## 五、状态栏和界面

### TEST-5.1: 上下文占用率

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 5.1.1 | 进行一次对话 | 状态栏显示 `X%/YK` 格式上下文使用率 |
| 5.1.2 | 进行大量对话（10+ 轮） | 百分比逐渐上升 |
| 5.1.3 | 颜色变化 | <70% 绿色，70-90% 黄色，>90% 红色 |

### TEST-5.2: Todo 侧边栏

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 5.2.1 | 通过 todowrite 工具创建待办 | 右侧侧边栏显示 Todo 列表 |
| 5.2.2 | 窄终端（<70 列） | 侧边栏自动隐藏 |

### TEST-5.3: 运行状态显示

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 5.3.1 | Agent 推理中 | 状态栏显示 `Running...`（黄色） |
| 5.3.2 | Agent 空闲 | 状态栏显示 `Ready`（绿色） |
| 5.3.3 | 发生错误 | 状态栏显示错误信息（红色） |

---

## 六、会话持久化测试

### TEST-6.1: 会话自动保存

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 6.1.1 | 进行一次对话 | `.mycode/sessions/<sessionId>/messages.json` 文件存在 |
| 6.1.2 | 进行多次对话 | 文件持续更新 |

### TEST-6.2: 基于会话 ID 恢复

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 6.2.1 | 退出后，执行 `mycode -c <sessionId>` | 成功恢复，显示历史消息 |
| 6.2.2 | 输入新问题 | Agent 基于完整历史回应 |

### TEST-6.3: 无效会话 ID 恢复

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 6.3.1 | `mycode -c nonexistent-id` | 显示会话不存在或为空的错误提示 |

---

## 七、上下文压缩测试

### TEST-7.1: 自动压缩触发

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 7.1.1 | 进行多轮长对话（复制粘贴大量文本，10+ 轮） | — |
| 7.1.2 | 观察状态栏 | 当使用率超过 70% 且满足 3 轮冷却后，自动触发压缩 |
| 7.1.3 | 观察压缩事件 | 界面显示 `[此前对话已压缩]` 摘要信息 |
| 7.1.4 | 验证压缩效果 | 上下文使用率下降 |

### TEST-7.2: 手动压缩

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 7.2.1 | 有足够历史时，输入 `/compact` 回车 | 立即触发压缩，状态栏更新使用率 |

---

## 八、技能系统测试（如有配置）

### TEST-8.1: 技能触发

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 8.1.1 | 输入 `/skills` 查看可用技能 | 显示技能列表 |
| 8.1.2 | 输入 `/skill-name 你的问题` | Agent 按技能指引回应 |

---

## 九、MCP 工具测试

### TEST-9.1: MCP Web 搜索

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 9.1.1 | 输入 `搜索最新的 TypeScript 5.7 新特性` 回车 | ① 看到 MCP 工具调用（`web_search_prime` 等）② 返回搜索结果 |
| 9.1.2 | 输入 `读取 https://www.typescriptlang.org/ 的内容` 回车 | Web Reader MCP 返回页面内容 |

---

## 十、LLM Provider 兼容性

### TEST-10.1: OpenAI 格式

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 10.1.1 | 使用 `format: "openai"` 的 Provider | 对话正常，流式输出正常 |

### TEST-10.2: DeepSeek 原生格式

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 10.2.1 | 通过 `/connect` 添加 `format: "deepseek"` 的 Provider | 添加成功 |
| 10.2.2 | 通过 `/models` 切换到该 Provider | 切换成功 |
| 10.2.3 | 进行对话 | 对话正常，流式输出正常 |

### TEST-10.3: Anthropic 格式（如有）

| 步骤 | 操作 | 预期结果 |
|------|------|---------|
| 10.3.1 | 切换到 Anthropic 格式 Provider | 对话正常 |

---

## 测试清理

完成测试后：

```bash
# 清理测试文件
rm -f /tmp/mycode-test.txt

# 查看当前分支状态
git status

# 如需切换回 main 分支
git checkout main
```

---

## 问题记录模板

发现问题时，记录以下信息：

```
测试编号: TEST-X.X
问题描述: [简要描述]
复现步骤: [详细步骤]
预期结果: [应该发生什么]
实际结果: [实际发生了什么]
错误信息: [如有报错，粘贴完整错误]
状态栏信息: [上下文使用率、Provider 名称等]
```
